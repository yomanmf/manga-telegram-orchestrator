{
  "name": "manga-kindle-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "npm --prefix apps/manga-bot-worker run check && npm --prefix apps/manga-pdf-processor run check && npm --prefix apps/kindle-uploader run check",
    "test": "npm --prefix apps/manga-bot-worker test && npm --prefix apps/manga-pdf-processor test && npm --prefix apps/kindle-uploader test",
    "verify": "npm run check && npm test"
  },
  "devDependencies": {
    "railway": "^3.5.5"
  }
}
